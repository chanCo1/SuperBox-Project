import React, { memo } from 'react';
import Meta from '../Meta';

const FaqPage = memo(() => {
  return (
    <div>
      <Meta title={'SuperBox :: 자주찾는 질문'} />
      고객센터
    </div>
  );
});

export default FaqPage;