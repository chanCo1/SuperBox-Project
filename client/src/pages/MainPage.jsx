import React, { memo } from 'react';

import Meta from '../Meta';

const MainPage = memo(() => {
  return (
    <div>
      <Meta title={'SuperBox :: 메인페이지'} />
    
    </div>
  );
});

export default MainPage;